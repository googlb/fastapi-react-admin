import { LoginForm } from '@/components/LoginForm';

export const Login = () => {
    return (
        <div className="flex flex-col items-center gap-4">
            <LoginForm />
        </div>
    );
};
